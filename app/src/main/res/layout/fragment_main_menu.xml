<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/descriptionScroll"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#454441"
    android:scrollbars="vertical">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- <TextView
            android:id="@+id/MainTitle"
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"
            android:textColor="@drawable/white"
            android:layout_marginLeft="5dip"
            android:layout_marginBottom="5dip"
            android:textSize="16sp"/> -->

        <ImageView
            android:id="@+id/WalkingTourButton"
            android:layout_height="match_parent"
            android:layout_width="match_parent"
            android:background="#1f2021"
            android:adjustViewBounds="true"
            android:contentDescription="@string/walking_tours"
            android:src="@drawable/walking_tours" />

        <ImageView
            android:id="@+id/SearchNearbyButton"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:background="#1f2021"
            android:contentDescription="@string/search_nearby"
            android:src="@drawable/search_nearby" />

        <ImageView
            android:id="@+id/ListButton"
            android:layout_height="match_parent"
            android:layout_width="match_parent"
            android:background="#1f2021"
            android:adjustViewBounds="true"
            android:contentDescription="@string/building_list"
            android:src="@drawable/list_of_buildings" />

        <!-- <ImageView
                android:id="@+id/MapButton"
             android:layout_height="wrap_content"
              android:layout_width="wrap_content"
              android:layout_weight="3"
              android:text="View Map"
              android:src="@drawable/map_but" /> -->

        <ImageView
            android:id="@+id/PhotosButton"
            android:layout_height="match_parent"
            android:layout_width="match_parent"
            android:background="#1f2021"
            android:adjustViewBounds="true"
            android:contentDescription="@string/view_photos"
            android:src="@drawable/photo_gallery"/>

        <!-- <ImageView
            android:id="@+id/VirtualButton"
            android:layout_height="wrap_content"
             android:layout_width="wrap_content"
             android:text="Locate Nearby"
             android:src="@drawable/virtual_but"/> -->
    </LinearLayout>
</ScrollView>